// Ajouter dans "dataGroups" pour un cache offline des tuiles :
/*
{
  "name": "mapbox-api",
  "urls": ["https://api.mapbox.com/**"],
  "cacheConfig": {
    "maxSize": 100,
    "maxAge": "7d",
    "timeout": "10s",
    "strategy": "performance"
  }
},
{
  "name": "mapbox-events",
  "urls": ["https://events.mapbox.com/**"],
  "cacheConfig": {
    "maxSize": 10,
    "maxAge": "1d",
    "timeout": "5s",
    "strategy": "freshness"
  }
}
*/